# Reproducibility

Record the exact Git commit, Python version, configuration, scenario, seed, duration, attack window, detector thresholds, twin uncertainty, and all parameter changes. Synthetic outputs must not be described as measurements from a real railway.
