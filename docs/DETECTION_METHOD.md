# Detection Method

The baseline detector combines digital-twin residuals, block mismatches, timestamp/sequence freshness, and discrete-state consistency. Source trust falls when anomaly scores persist and gradually recovers during healthy observations.

The detector is intentionally interpretable and should be treated as a benchmark baseline, not a production IDS.
