# Benchmark Protocol

Each scenario should report detection precision/recall/F1, false-positive rate, source trust, safety-invariant violations, completion ratio, restriction fraction, and resilience score.

Use fixed seeds for baseline comparisons, repeat with multiple seeds for research claims, and report attack windows and model uncertainty.
